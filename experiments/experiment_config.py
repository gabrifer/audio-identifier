class ExperimentConfig:
    
    def __init__(self, name, features, algorithm) -> None:
       self.name = name
       self.features = features
       self.algorithm = algorithm
       
        
     